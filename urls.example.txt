# Replace this placeholder with an authorized highest-priority source URL.
# A queue URL must use the path: /<collection>/data/<file path>
https://example.invalid/case/data/replace-with-authorized-file-1.bin
